
import 'dart:ui';

class AchievementModel{
  String imgUrl;
  Color bgColor;
  Color iconColor;
  String title;
  String subtitle;

  AchievementModel(
      this.imgUrl, this.bgColor, this.iconColor, this.title, this.subtitle);
}