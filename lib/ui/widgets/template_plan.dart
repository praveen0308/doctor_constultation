import 'package:flutter/cupertino.dart';

class TemplatePlan extends StatelessWidget {
  const TemplatePlan({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
